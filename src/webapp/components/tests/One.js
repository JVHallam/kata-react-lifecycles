import React from "react";

export default function( { KataComponent } ){
  return (
    <React.Fragment>
      <p> Test One : Check that the default component renders </p>
      <KataComponent />
    </React.Fragment>
  );
};
